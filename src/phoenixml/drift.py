"""Evidently-based drift detection (Phase 3 — stub)."""

from __future__ import annotations

# Full implementation in Phase 3.
# Stub keeps imports clean for CI.


def compute_drift(reference_df, current_df, settings=None) -> dict:  # type: ignore[return]
    raise NotImplementedError("drift.compute_drift implemented in Phase 3")
