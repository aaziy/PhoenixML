"""Monitoring loop: PR-AUC + drift detection (Phase 3 — stub)."""

from __future__ import annotations

# Full implementation in Phase 3.


def run_monitor(batch_id: int = 0, settings=None) -> dict:  # type: ignore[return]
    raise NotImplementedError("monitor.run_monitor implemented in Phase 3")
